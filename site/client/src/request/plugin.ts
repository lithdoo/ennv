
import { ERequset } from "@/utils/request";

export const getLoadScripts = 
    // () => ERequset.get('/load/script/list')
    // .send().json() as Promise<string[]>
    async ()=>([])

export const getLoadStylesheets = 
    // () => ERequset.get('/load/stylesheet/list')
    // .send().json() as Promise<string[]>
    async ()=>([])
